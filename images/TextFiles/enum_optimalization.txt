for(int i = 0; i < 8; i++){
    for(int j = 0; j < 8; j++){
        // Utworzenie nowego przycisku i przypisanie obrazka
        board[i][j] = new wxBitmapButton(this, wxNewId(), images[color][type], wxDefaultPosition, wxDefaultSize, wxBU_AUTODRAW, wxDefaultValidator);
        
        // Dodanie przycisku do siatki
        GridSizer1->Add(board[i][j], 1, wxALIGN_CENTER_HORIZONTAL|wxALIGN_CENTER_VERTICAL, 5);
        
        // Połączenie obsługi zdarzenia kliknięcia przycisku z funkcją OnBitmapButton1Click
        Connect(board[i][j]->GetId(), wxEVT_COMMAND_BUTTON_CLICKED, (wxObjectEventFunction)&Chess2DDialog::OnBitmapButton1Click);
    }
}